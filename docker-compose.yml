services:
  web:
    build:
      context: apps/web
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./apps/web/src:/app/src
    environment:
      - NEXT_PUBLIC_DATA_API_URL=http://localhost:8080/api/pricing
    env_file:
      - path: apps/web/.env
        required: false

  api:
    build:
      context: apps/api
      dockerfile: Dockerfile
      target: dev
    ports:
      - "8080:8080"
    env_file:
      - path: apps/api/.env
        required: false
